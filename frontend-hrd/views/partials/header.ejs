<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8" />
  <title><%= title || "Dashboard HRD" %></title>

  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    rel="stylesheet"
  />
</head>
<body class="bg-light">

<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <a class="navbar-brand" href="/dashboard">HRD</a>

    <ul class="navbar-nav ms-auto">
      <li class="nav-item">
        <%
  const current =
    typeof active !== "undefined"
      ? active
      : "";
%>

        <a class="nav-link <%= current === 'dashboard' ? 'active' : '' %>" href="/dashboard">
  Dashboard
</a>
      </li>
      <li class="nav-item">
        <a class="nav-link <%= current === 'karyawan' ? 'active' : '' %>" href="/karyawan">
  Karyawan
</a>
      </li>
      <li class="nav-item">
        <a class="nav-link text-danger" href="/logout">Logout</a>
      </li>
    </ul>
  </div>
</nav>

<div class="container mt-4">
